# Secrets / credentials — never commit
*.env
.env*
!.env.example
*.pem
*.key
credentials*

# Local config / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node / Python caches (future code)
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build outputs (future)
dist/
build/
coverage/

# DDE runtime data (provenance/traces are local records, append-only; not committed)
product/drug-discovery-engine/app/data/